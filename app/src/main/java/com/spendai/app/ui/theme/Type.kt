package com.spendai.app.ui.theme

import androidx.compose.material3.Typography

/**
 * Typography for SpendAI. We stick to M3 defaults (and the size scale M3
 * derives from the platform text appearance) so the UI feels native on
 * every device. Headlines and titles are slightly tightened via the
 * platform defaults — no custom display sizes.
 */
val SpendAiTypography = Typography()
